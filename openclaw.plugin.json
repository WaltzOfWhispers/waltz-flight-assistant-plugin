{
  "id": "waltz-flight-assistant",
  "name": "Waltz Flight Assistant",
  "description": "Book flights end to end, in conversation. Waltz learns your preferences and traveler info over time, so you stop re-entering everything.",
  "version": "0.1.3",
  "skills": ["./skills"],
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": ["flight_assistant"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "baseUrl": {
        "type": "string",
        "description": "Base URL for the hosted Waltz Flight Assistant backend. Current public endpoint: https://waltz-flight-staging.up.railway.app"
      },
      "authToken": {
        "type": "string",
        "description": "Optional bearer token sent to the hosted flight agent"
      },
      "openclawId": {
        "type": "string",
        "description": "Preferred stable OpenClaw user id forwarded to the hosted flight agent"
      },
      "userId": {
        "type": "string",
        "description": "Legacy alias for openclawId. Prefer openclawId for new installs."
      },
      "requestTimeoutMs": {
        "type": "integer",
        "minimum": 1000,
        "description": "HTTP timeout for calls to the hosted flight agent"
      }
    }
  },
  "uiHints": {
    "baseUrl": {
      "label": "Waltz Flight URL",
      "placeholder": "https://waltz-flight-staging.up.railway.app",
      "help": "Hosted base URL for the Waltz Flight Assistant backend. Use https://waltz-flight-staging.up.railway.app unless Waltz provides a different deployment."
    },
    "authToken": {
      "label": "Bearer Token",
      "sensitive": true,
      "advanced": true,
      "help": "Optional bearer token for protected hosted deployments."
    },
    "openclawId": {
      "label": "OpenClaw ID",
      "advanced": true,
      "placeholder": "openclaw_user_123"
    },
    "userId": {
      "label": "User ID",
      "advanced": true,
      "placeholder": "legacy_user_123"
    },
    "requestTimeoutMs": {
      "label": "Request Timeout (ms)",
      "advanced": true,
      "placeholder": "30000"
    }
  }
}
